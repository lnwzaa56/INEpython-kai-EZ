def is_armstrong(numbers):
    digits = str(numbers)
    Len_num = len(digits)
    total=0
    for n in num:
        num1 = int(n)
        num1 += n** Len_num

    if digits == n :
        print(f"{num} is not an Armstrong number")

    else:
        print(f"{num} is not an Armstrong number")


   # def is_armstrong(numbers):
        # digits = str(num)
        # power = len(digits)
        # total=0
        #for digit in digits:
           #total =total + int(digit)**power

        #if total == num:
           #return True
        #else:
           #return False
 
    
     
 





    
    
